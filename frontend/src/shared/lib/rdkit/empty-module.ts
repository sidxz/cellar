// Empty module stub for Node.js built-ins (fs, path, etc.) that
// RDKit.js references in its Node.js detection path. The WASM module
// uses fetch in browser context, so these are never actually called.
export default {};
