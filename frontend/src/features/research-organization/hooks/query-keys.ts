/**
 * Shared React Query root keys for the research-organization feature.
 * Declared once here so invalidation and reads stay in lockstep across hooks.
 * Mirrors chemical-registration/hooks/query-keys.ts.
 */

export const COLLECTIONS_KEY = ["collections"];
