"use client";

interface FilesTabProps {
  protocolId: string;
}

export function FilesTab({ protocolId }: FilesTabProps) {
  return (
    <div className="p-8 text-center text-muted-foreground">
      Files tab — coming in next task
    </div>
  );
}
